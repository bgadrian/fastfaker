language: go
codecov: true

go:
  - "1.0"
  - master

install:
- make

script:
- go test -coverprofile=coverage.txt -covermode=atomic github.com/bgadrian/fastfaker/faker github.com/bgadrian/fastfaker/data github.com/bgadrian/fastfaker/randomizer

after_success:
- bash <(curl -s https://codecov.io/bash)